# Test dependencies for the HCC Compression Advisor dashboard.
# The tests import the application, so install its requirements as well:
#   pip install -r requirements.txt -r requirements-test.txt
#   python -m pytest            (from python/; options in pytest.ini)

pytest>=7.4.0
# --cov options in pytest.ini (pulls in coverage)
pytest-cov>=4.1.0
